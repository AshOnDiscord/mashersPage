{
  "devDependencies": {
    "tailwindcss": "^3.0.24"
  },
  "scripts": {
    "start": "npx tailwindcss -i ./input.css -o ./output.css --watch",
    "minify": "npx tailwindcss -i ./input.css -o ./output.css --watch --minify"
  }
}
